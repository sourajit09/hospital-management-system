# Leave empty for local dev: Vite proxies /api → http://localhost:4000 (see vite.config.js).
# Set to your real API when the admin UI and API are on different hosts (e.g. production).
VITE_BACKEND_URL=
